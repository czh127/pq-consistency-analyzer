# PQ 产品一致性检测分析程序 - 依赖清单
# 安装命令：pip install -r requirements.txt

# 数据处理
pandas>=1.3.0
numpy>=1.21.0

# 图表绘制
matplotlib>=3.5.0

# Excel 报告生成（含内嵌散点图）
openpyxl>=3.0.0

# 图片查看器（v6.2 及以上版本需要）
pillow>=9.0.0

# 说明：
# - tkinter 为 Python 标准库，无需 pip 安装
#   Linux 系统如缺失可执行：sudo apt install python3-tk
# - Python 版本要求：>= 3.9
